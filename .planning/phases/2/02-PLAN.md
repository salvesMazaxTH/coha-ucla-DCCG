# Phase 2 Plan

1. Reescrever `DeckValidator` com `validateDetailed` e erros estruturados.
2. Garantir decodificacao cross-runtime de deck code (`DeckCoder`).
3. Expor endpoint backend para validacao formal de deck.
4. Integrar rejeicao/aceitacao no protocolo de socket.
