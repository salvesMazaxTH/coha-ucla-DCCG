# Phase 2 Summary

`server/core/DeckValidator.js` agora aplica 48 cartas, limites por copia, coesao por essencia e retorno estruturado (`rule`, `message`, `details`).

`server/server.js` expõe `/api/decks/validate` com resposta autoritativa do backend.
